import React from 'react';

const Search = () => (
    <section className='search__container'>
        City: <input type='search' placeholder='autocomplete search' />
    </section>
);

export default Search;
